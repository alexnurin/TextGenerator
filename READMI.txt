Инструкция:
Распоковать data в папку с 'generator.py'
Радоваться жизни:
Полноценная работа - запустить menu.py
Отдельно работают generator.py и scaner.py
Доступ и изменение переменных - params.py

Детали работы:
Деление на предложение поддерживается. Во избежание конфликтов символ "." заменен на "1" в данных
Русский язык полностью поддерживается, но буква "ё" заменяется на "е"
Настройки пока только в виде ручного изменения файла params.py - там кол-во слов в генерации (n) и кол-во слов в 1 строке (newString),
а также bigSet, получающий данные из names.txt в этой же папке. Это - список названий и имён, то есть слова с большой буквой независимо
В menu.py реализована работа через консоль как со сканером, так и с генератором.

Выложил данные в виде архива, потому что 19664 файлика вгружать 197 раз, это печально. Поэтому архив следует распаковать в папку с фалом 'generator.py'. Обучен на ведьмаке (полная книга "Час Презрения", Сапковский А.), можно обучать на чём угодно (логично)
